
##1. 项目定位

这是一个企业级 Java / Spring Cloud 业务系统。

本项目同时维护一个基于 Obsidian 的工程知识库，用于沉淀：

- 业务领域知识
    
- 系统架构
    
- 模块职责
    
- 核心业务流程
    
- 技术实现
    
- 设计决策
    
- 历史问题与踩坑
    
- 重要代码变更
    
- 修改代码时需要注意的隐含约束
    

### 核心原则

代码仓库是当前系统实际行为的主要事实来源。

Obsidian 知识库是经过人工确认后形成的长期工程知识。

当代码与知识库内容冲突时：

1. 不要自行假设哪个是正确的。
    
2. 优先分析当前代码的实际行为。
    
3. 明确指出代码与知识库之间的冲突。
    
4. 如果无法确认业务意图，将其标记为待确认，而不是自行编造结论。
    

---

# 2. 技术栈

项目主要使用：

- Java
    
- Spring Boot
    
- Spring Cloud
    
- MyBatis / MyBatis Plus
    
- MySQL
    
- ShardingSphere
    
- RocketMQ
    
- RabbitMQ
    
- Redis
    
- Elasticsearch
    
- MinIO
    
- Docker
    
- Vue3
    
- Vite
    
- Element Plus
    

具体版本以及实际使用情况以项目代码和构建文件为准，不要仅依据此文件推断。

---

# 3. 工作方式

## 3.1 修改代码之前

在修改业务代码之前，应先理解：

1. 当前需求涉及的业务领域
    
2. Controller / API 入口
    
3. Service 调用链
    
4. Mapper / Repository
    
5. 数据库表
    
6. MQ / 异步任务
    
7. 定时任务
    
8. 事务边界
    
9. 分布式锁
    
10. 缓存
    
11. 其他调用方
    

不要只修改当前看到的方法，而忽略其调用关系和副作用。

---

## 3.2 搜索代码

在进行修改之前，应优先搜索：

- 方法定义
    
- 方法所有调用方
    
- 类的所有引用
    
- DTO / VO / Entity 的使用位置
    
- MQ Topic / Tag
    
- 定时任务
    
- 数据库表
    
- SQL
    
- 配置项
    
- 相关测试代码
    

对于业务逻辑修改，应尽量确认完整调用链。

---

## 3.3 修改范围

遵循最小修改原则。

除非任务明确要求：

- 不要重构无关代码
    
- 不要修改无关格式
    
- 不要删除看起来“没用”的代码
    
- 不要修改公版遗留代码
    
- 不要顺便升级依赖
    
- 不要为了代码风格而大范围修改文件
    

如果发现明显可以优化但与当前任务无关的问题，应记录并提出，而不是直接修改。

---

# 4. 特别注意公版遗留代码

本项目可能存在从公共版本继承而来的：

- 无实际业务使用的字段
    
- 无实际调用的方法
    
- 已废弃但仍然保留的逻辑
    
- 与当前业务不一致的注释
    
- 历史兼容代码
    

因此：

**不能仅因为某个字段、方法或类存在，就认为它属于当前实际业务流程。**

判断一个功能是否真实生效，应尽量结合：

1. 调用关系
    
2. 实际入口
    
3. 数据库使用情况
    
4. 配置
    
5. MQ
    
6. 实际业务流程
    
7. 测试
    
8. 最近的代码变更
    

如果无法确定，应明确标记：

> 未确认是否属于当前有效业务逻辑。

不要把推测写成事实。

---

# 5. Obsidian 知识库

知识库位于：

`工作知识库路径`

目录结构：

```text
01-业务/
02-系统/
03-模块/
04-技术/
05-问题与踩坑/
06-设计决策/
07-变更记录/
90-待确认/
99-临时/
```

## 5.1 知识库的职责

知识库不是代码镜像。

不要把大量 Java 类、Getter、Setter、普通 CRUD 逻辑复制进知识库。

应该优先记录：

- 业务规则
    
- 业务流程
    
- 模块职责
    
- 系统边界
    
- 数据流
    
- 核心依赖
    
- 设计原因
    
- 隐含约束
    
- 容易出错的地方
    
- 历史问题
    
- 重要技术决策
    

---

# 6. 知识库可信度

知识库中的内容具有不同可信度。

使用 Properties 区分：

```yaml
status: confirmed
```

表示已经确认。

```yaml
status: draft
```

表示 AI 或人工整理出的草稿，尚未确认。

```yaml
status: deprecated
```

表示历史知识，不应作为当前系统行为依据。

```yaml
status: pending
```

表示存在不确定性，需要人工确认。

### 重要规则

AI 推断出的信息不得直接标记为 `confirmed`。

如果无法从代码、配置、测试、历史记录或用户明确说明中确认，应使用：

```yaml
status: pending
```

---

# 7. 知识库更新原则

当修改代码后，如果修改影响已有知识，应检查相关 Markdown。

重点检查：

- 业务流程
    
- 模块职责
    
- API
    
- 数据库
    
- MQ
    
- 事务
    
- 并发
    
- 分布式锁
    
- 缓存
    
- 设计决策
    
- 历史问题
    

不要无条件修改所有相关文档。

只有确认代码行为发生变化时才更新。

---

# 8. 知识库中的链接

优先使用 Obsidian Wiki Link：

```markdown
[[采购]]
[[采购调整]]
[[库存]]
[[物流计划]]
[[RocketMQ]]
```

当某个知识与另一个知识存在明显关系时，应建立链接。

例如：

```markdown
相关业务：

[[采购]]
[[库存]]

相关问题：

[[采购调整税率为空]]
```

避免大量使用普通文本重复描述相同知识。

---

# 9. 新知识的处理流程

当发现值得长期保存的知识时：

```text
发现知识
    ↓
判断是否已有相关笔记
    ↓
有 → 更新已有笔记
无 → 创建新笔记
    ↓
判断知识来源
    ↓
代码 / 测试 / 明确业务规则
    ↓
confirmed

无法确认
    ↓
pending
    ↓
放入 90-待确认
```

不要因为发现一个技术细节就创建大量碎片化笔记。

优先复用已有知识。

---

# 10. 问题与踩坑记录

重要 Bug、事故和排查过程应优先记录。

建议包含：

```markdown
---
type: incident
status: resolved
date: YYYY-MM-DD
domain:
---

# 问题标题

## 现象

## 影响

## 根因

## 排查过程

## 修复方案

## 为什么容易发生

## 后续注意事项

## 相关代码

## 相关知识
```

重点记录：

> 为什么这个问题容易发生，以及以后修改时如何避免再次发生。

而不是只记录最终修复代码。

---

# 11. 设计决策

重要设计选择应记录原因。

例如：

- 为什么使用 MQ
    
- 为什么使用分布式锁
    
- 为什么不能修改某个字段
    
- 为什么某个查询必须走特定数据源
    
- 为什么某个操作禁止并发
    
- 为什么某个接口不能直接删除
    
- 为什么存在特殊的数据校验
    

推荐格式：

```markdown
---
type: decision
status: confirmed
date: YYYY-MM-DD
domain:
---

# 决策标题

## 背景

## 决策

## 原因

## 被放弃的方案

## 影响

## 相关代码

## 相关知识
```

---

# 12. 代码与知识库冲突

如果发现：

```text
代码行为 ≠ 知识库描述
```

不要直接修改知识库来“适应代码”。

先输出：

```text
发现知识冲突：

知识库：
xxx

当前代码：
xxx

冲突位置：
xxx

当前无法确认：
xxx
```

然后等待人工确认。

如果确认代码已经发生变更，应更新知识库，并更新：

```yaml
last_verified: YYYY-MM-DD
```

---

# 13. 分析任务

当用户要求：

> 分析某个模块

应优先输出：

1. 模块职责
    
2. 主要入口
    
3. 核心调用链
    
4. 核心数据表
    
5. 外部依赖
    
6. MQ / 异步逻辑
    
7. 事务边界
    
8. 并发控制
    
9. 核心业务规则
    
10. 潜在风险
    
11. 当前知识库中缺失的知识
    

不要一开始就生成大量 Markdown。

先分析，再整理。

---

# 14. 知识库生成任务

当用户要求生成知识库时：

1. 先读取现有知识库。
    
2. 再分析代码。
    
3. 尽可能复用已有笔记。
    
4. 避免创建重复知识。
    
5. 区分事实和推断。
    
6. 不确定内容标记为 `pending`。
    
7. 不要将公版遗留逻辑自动视为有效业务。
    
8. 不要为了让文档看起来完整而编造内容。
    

---

# 15. 代码修改后的知识审查

当用户要求：

> 检查本次修改是否影响知识库

执行：

```text
git diff
    ↓
识别修改的业务模块
    ↓
识别影响的调用链
    ↓
搜索相关 Obsidian 笔记
    ↓
检查：
    - 业务流程
    - 模块说明
    - 设计决策
    - 问题记录
    - API
    - 数据库
    ↓
输出：
    - 不需要更新
    - 建议更新
    - 明显过期
    - 无法确认
```

不要直接修改知识库，除非用户明确要求。

---

# 16. 用户要求“只分析”时

如果用户要求：

- 分析
    
- 检查
    
- 排查
    
- 评估
    
- 找问题
    
- 给建议
    

默认：

**不要修改代码，也不要修改知识库。**

只提供分析结果。

---

# 17. 用户要求修改时

如果用户明确要求修改：

1. 先分析影响范围。
    
2. 再执行修改。
    
3. 修改后检查 diff。
    
4. 确认没有产生无关变更。
    
5. 汇报修改内容。
    
6. 如果存在无法确认的问题，明确指出。
    

---

# 18. 输出原则

回答技术问题时：

- 结论优先
    
- 区分事实、推断和建议
    
- 不确定时明确说明
    
- 不要假装知道不存在的信息
    
- 尽量引用具体文件、类、方法和行号
    
- 对重要判断说明依据
    

避免：

- 空泛的架构描述
    
- 重复代码内容
    
- 没有依据的业务推测
    
- 为了完整而生成大量文档
    
- 把历史代码当作当前有效逻辑
    

---

# 19. 最重要的原则

始终遵循：

> **代码负责事实，知识库负责知识，人工负责最终确认。**

以及：

> **宁可留下“待确认”，也不要把错误的信息写入长期知识库。**